#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Description: that prints the alphabet in lowercase
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
int num;

for (num = 0; num < 10; num++)
{
putchar(n);
}
putchar("\n);
return (0);
}

